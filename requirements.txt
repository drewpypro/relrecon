# Core dependencies
polars>=1.0
rapidfuzz>=3.0
openpyxl>=3.1
pyyaml>=6.0

# Optional: international address parsing (better accuracy, requires C library)
# Install libpostal first: see docs/implementation-plan.md#libpostal-installation
# postal>=1.1
